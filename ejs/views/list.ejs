<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        ejs
    </title>
</head>

<body>
    <div>
        <% if (productos.length> 0) { %>
            <table>
                <tr>
                    <th>Nombre</th>
                    <th>Precio</th>
                    <th>Imagen</th>
                </tr>
                <% for(let producto of productos) { %>
                    <tr>
                        <td>
                            <%=producto.nombre %>
                        </td>
                        <td>
                            <%=producto.precio %>
                        </td>
                        <td>
                            <img src=<%=producto.imagen %> style="width: 50px;"></img>
                        </td>
                    </tr>
                    <% }%>

            </table>
            <% } else { %>
                <span>No hay productos para mostrar</span>
                <% } %>

    </div>
    <a href="/">Add Product</a>
</body>

</html>